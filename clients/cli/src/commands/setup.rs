use structopt::StructOpt;

#[derive(StructOpt, Debug)]
pub enum PackCommand{
    
}

#[derive(StructOpt, Debug)]
pub enum PolicyCommand{
    
}

#[derive(StructOpt, Debug)]
pub enum PolicyTypeCommand{
    
}

#[derive(StructOpt, Debug)]
pub enum RunnerCommand{
    
}

#[derive(StructOpt, Debug)]
pub enum ServiceRegistryCommand{
    
}