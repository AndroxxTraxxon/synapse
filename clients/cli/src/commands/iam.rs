use structopt::StructOpt;



#[derive(StructOpt, Debug)]
pub enum AuthCommand{
    
}

#[derive(StructOpt, Debug)]
pub enum LoginCommand{
    
}

#[derive(StructOpt, Debug)]
pub enum WhoAmICommand{
    
}

#[derive(StructOpt, Debug)]
pub enum ApiKeyCommand{
    
}

#[derive(StructOpt, Debug)]
pub enum RoleCommand{
    
}

#[derive(StructOpt, Debug)]
pub enum RoleAssignmentCommand{
    
}
